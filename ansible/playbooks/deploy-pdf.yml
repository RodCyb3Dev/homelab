##############################################################################
# ANSIBLE PLAYBOOK - Deploy PDF Management
# Deploys Paperless-ngx document management
##############################################################################

---
- name: Deploy PDF Management Stack
  hosts: production
  become: yes
  become_user: root
  gather_facts: yes
  
  vars_files:
    - vault.yml
  
  vars:
    deploy_path: "{{ hostvars[inventory_hostname]['deploy_path'] | default('/opt/homelab') }}"
    compose_file_name: "docker-compose.pdf.yml"
    compose_file_path: "{{ deploy_path }}/{{ compose_file_name }}"
    compose_file_requires_tailscale: true

  pre_tasks:
    - name: Ensure deployment directory exists
      file:
        path: "{{ deploy_path }}"
        state: directory
        owner: deploy
        group: deploy
        mode: '0755'

  roles:
    - role: install_homelab_tools
      tags: ['install', 'tools']

    - role: sync_config
      tags: ['config', 'sync']

    - role: sync_secrets
      tags: ['secrets', 'sync']

    - role: deploy_compose
      tags: ['deploy', 'services']

  post_tasks:
    - name: Display deployment summary
      debug:
        msg:
          - "✅ PDF management stack deployment completed!"
          - "📊 Services: {{ deploy_path }}/{{ compose_file_name }}"

